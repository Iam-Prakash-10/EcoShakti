services:
  - type: web
    name: ecoshakti
    env: python
    buildCommand: pip install -r requirements.txt && mkdir -p logs uploads
    startCommand: chmod +x start.sh && ./start.sh
    envVars:
      - key: FLASK_ENV
        value: production
      - key: PYTHON_VERSION
        value: 3.11.0